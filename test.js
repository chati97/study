


/*

function adding(){
    const listval = document.getElementById("list").value;
    const di = document.createElement("div");
    di.setAttribute('id', 'di');
    const textNode = document.createTextNode(listval);
    di.appendChild(textNode);
    const deb = document.createElement("button");
    const btntxt = document.createTextNode('X');
    deb.appendChild(btntxt);
    // deb.setAttribute("onclick", "this.parentNode.remove()");
    di.appendChild(deb);
    document.body.appendChild(di);

    di.addEventListener("click", () => {

    })
}

// SPA : Single Page Application
// Event Bubbling, Capturing, Delegation
// 이벤트 버블링, 이벤트 캡쳐링, 이벤트 위임

// HTML, CSS 틈틈이 공부하면 좋아.



const rootElement = document.getElementById("root")

const input = document.createElement("input");

rootElement.appendChild(input)

*/